<div class='ui huge center aligned red header'> YOUR PERSONAL LISTS UPON LISTS UPON LISTS.</div>
<div class='ui divided list'>
<% @lists.each do |list| %>
  <div class='item'>
    <i class="circular bolt icon"></i>
    <div class='content'>
        <div class='header'>
          <%= link_to list.title, list_path(list.id) %>
        </div>
        <%= list.description %>
      </div>
    </div>
  <% end %>
</div>

  <%= link_to 'Make a new list!', new_list_path %>