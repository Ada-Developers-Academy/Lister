<h2> Here's your lists, your own personal lists! </h2>
<% @lists.each do |list| %>
  <h3><%= link_to list.title, list_path(list.id) %></h3>
  <p><%= list.description %></p>
  <% end %>

  <%= link_to 'Make a new list!', new_list_path %>