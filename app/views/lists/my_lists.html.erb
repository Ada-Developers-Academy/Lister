<h2>Here You Can See All Of Your Lists</h2>

<%= link_to "Create New List", new_list_path, :class => "btn btn-default" %>

<hr> 

<% @current_user.lists.each do |list| %>
  <p><%= link_to "#{list.name}", list_path(list.id) %></p>
<% end %>