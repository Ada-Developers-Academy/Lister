<h1>Lists</h1>

<ul>
  <% @lists.each do |list| %>
  <li><%= link_to "#{list.name}", list_path(list) %>
    <ul>
      <% list.items.each do |item| %>
        <li><%= item.name %></li>
      <% end %>
    </ul>
  </li>
  <% end %>

</ul>

<%= @lists.inspect %>